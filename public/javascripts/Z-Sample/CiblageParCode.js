//CODE

if (jQuery(".breadcrumb li:contains(CHANEL)").length > 0)
{return true;}
else {return false;}

// Bien indiquer le return dans AB Tasty. 
// En testant ce code dans la console il y aura une erreur, car on ne peut pas indiquer de return dans la console.
// Si on les enlève dans AB tasty cela ne fonctionnera plus.
